
<%= form_with(model: asesor, local: true) do |form| %>
  <% if asesor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asesor.errors.count, "error") %> prohibited this asesor from being saved:</h2>

      <ul>
        <% asesor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, {:class => "form-control", :placeholder => "Ingrese nombre", :requerid =>
      "requerid"} %>
  </div>

  <div class="field">
    <%= form.label :apellido %>
<%= form.text_field :apellido, {:class => "form-control", :placeholder => "Ingrese apellido", :requerid =>
      "requerid"} %> 
  </div>

  <div class="field">
    <%= form.label :Usuario %>   
    <%= form.text_field :user, {:class => "form-control", :placeholder => "Ingrese Usuario", :requerid =>
      "requerid"} %> 
  </div>

  <div class="field">
    <%= form.label :password %>
      <%= form.text_field :password, {:class => "form-control", :placeholder => "Ingrese Contraseña", :requerid =>
      "requerid"} %> 
  </div>

  <div class="actions">
    <%= form.submit(class:"btn btn-success")%>
  </div>
<% end %>
