<%= form_with(model: proyecto, local: true) do |form| %>
  <% if proyecto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proyecto.errors.count, "error") %> prohibited this proyecto from being saved:</h2>

      <ul>
        <% proyecto.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<nav aria-label="Page navigation example">
  <ul class="pagination">
    <li class="page-item"><a class="page-link" href="#">Previous</a></li>
    <li class="page-item"><a class="page-link" href="#">1</a></li>
    <li class="page-item"><a class="page-link" href="#">2</a></li>
    <li class="page-item"><a class="page-link" href="#">3</a></li>
    <li class="page-item"><a class="page-link" href="#">Next</a></li>
  </ul>
</nav>
        <div class="form-group">
          <%= form.text_field :nombre_proy, { :class => "form-control", :placeholder => "Ingrese nombre de proyecto" } %>
        </div>
        <div class="form-group">
          <%= form.text_area :descrip_proy, { :class => "form-control", :placeholder => "Descripción"  } %>
        </div>
        <div class="form-group">
          <%= form.text_area :ubicacion_proy, { :class => "form-control", :placeholder => "Ubicación"  } %>
        </div>

        <div class="form-group">
          <%= form.text_field :precio_base, { :class => "form-control", :placeholder => "Precio base"  } %>
        </div>

        <div>
          <th>Cargar imágen</th>
          <td>
            <%=form.label :Imagen %>
             <%= form.file_field :foto_proy, multiple: true, accept: 'images/*' %>
          </td>
          
        </div>

  <div class="actions">
    <%= form.submit "Guardar", :class=> "btn btn-success" %>
  </div>
<% end %>
