
<%= form_with(model: estado, local: true) do |form| %>
  <% if estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estado.errors.count, "error") %> prohibited this estado from being saved:</h2>

      <ul>
        <% estado.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Id %>   
      <%= form.number_field :icodigo, {:class => "form-control", :placeholder => "Ingrese id", :requerid =>
      "requerid"} %> 
  </div>

  <div class="field">
    <%= form.label :Descripción %>
    <%= form.text_field :descripcion, {:class => "form-control", :placeholder => "Ingrese descripcion", :requerid =>
      "requerid"} %> 
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
